import styled from 'styled-components';

import {
  SignInFormContainer,
  SignInTitle,
  SignInSubTitle,
} from '../signInForm/signInForm.styles';

export const SignUpFormContainer = styled(SignInFormContainer)``;

export const SignUpTitle = styled(SignInTitle)``;

export const SignUpSubTitle = styled(SignInSubTitle)``;
